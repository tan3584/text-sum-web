/*
 * Get an value of Object with key
 * Using to get value of an column from data Object that get from database
 * @param customType: Record<any, string>
 *
 * @return string
 */
export const getValueFromObject = (data: any, key: string): string => {
  return '';
};
